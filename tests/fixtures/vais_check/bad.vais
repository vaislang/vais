module demo
package demo
use math.add
import math::add
pub fn exposed() -> Int { return 1 }

fn missing_return(a: Int, b: Int) {
    return a + b
}

fn main(argc: Int) -> Int { return argc }

fn f(s: String) -> Int {
    let xs: Vec<Int> = []
    let ys = List<Int>::new()
    let zs = vec![1, 2, 3]
    let map: HashMap<Int, Int> = {}
    let maybe: Option<Str> = None
    let result: Result<Int, Str> = Err("bad")
    let generic_map: Map<Int, Str> = {}
    let casted = value as Int
    let small: i32 = 5
    let precise: f64 = 0
    let label = 42.to_string()
    let one = opt.unwrap()
    let two = res.expect("value")
    if !ready { return 3 }
    if let Some(v) = opt { return v }
    if a && b { return 1 }
    if a || b { return 2 }
    if a { return 3 } elsif b { return 4 }
    match c { Color::Red => return 5 }
    total += 1
    total -= 1
    total *= 2
    return 0
}
