# CHECKS: detect strings containing only ASCII alphanumeric.
F is_alnum(c: i64) -> i64 {
    I c >= 48 && c <= 57 { R 1 }
    I c >= 65 && c <= 90 { R 1 }
    I c >= 97 && c <= 122 { R 1 }
    0
}

F all_alnum(p: i64, n: i64) -> i64 {
    LF i: 0..n {
        I is_alnum(load_byte(p + i)) == 0 { R 0 }
    }
    1
}

F main() -> i64 {
    p1: i64 := str_to_ptr("Hello123")
    I all_alnum(p1, strlen(p1)) != 1 { R 1 }

    p2: i64 := str_to_ptr("hello world")
    I all_alnum(p2, strlen(p2)) != 0 { R 2 }

    p3: i64 := str_to_ptr("test!")
    I all_alnum(p3, strlen(p3)) != 0 { R 3 }

    p4: i64 := str_to_ptr("")
    I all_alnum(p4, 0) != 1 { R 4 }
    R 0
}
