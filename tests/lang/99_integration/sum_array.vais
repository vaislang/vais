# CHECKS: build a Vec, iterate via index, sum
U std/vec.{vec_new};

F main() -> i64 {
    v: Vec<i64> := mut vec_new()
    LF i: 1..=5 {
        v.push(i)
    }
    sum: i64 := mut 0
    LF i: 0..v.len() {
        sum = sum + v[i]
    }
    # 1+2+3+4+5 = 15
    I sum != 15 { R 1 }
    R 0
}
