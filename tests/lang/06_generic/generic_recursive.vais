# CHECKS: generic function calling itself recursively.
F count_down<T>(n: i64, init: T) -> T {
    I n <= 0 { init }
    EL { count_down(n - 1, init) }
}

F main() -> i64 {
    # Pass through 5 levels of recursion
    a: i64 := count_down(5, 42)
    I a != 42 { R 1 }
    b: i64 := count_down(0, 100)
    I b != 100 { R 2 }
    R 0
}
