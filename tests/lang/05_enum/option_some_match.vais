# CHECKS: Option<i64> Some(v)/None destructuring across multiple branches.
fn head_or_zero(v: Option<i64>) -> i64 {
    match v {
        Some(x) => x,
        None    => 0
    }
}

fn main() -> i64 {
    a: i64 := head_or_zero(Some(42))
    I a != 42 { return 1 }

    b: i64 := head_or_zero(None)
    I b != 0 { return 2 }

    c: i64 := head_or_zero(Some(0 - 7))
    I c != 0 - 7 { return 3 }
    return 0
}
