# Consumer: vais-server-style API handler returning email.
# See vaisdb_table.vais header for the cat-based concatenation note.

fn handler_email(u: User) -> str {
    return u.email
}

fn main() -> i64 {
    return 0
}
