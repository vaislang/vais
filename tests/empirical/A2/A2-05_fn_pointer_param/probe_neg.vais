fn double(x: i64) -> i64 { x * 2 }

fn apply(f: fn(i64) -> i64, n: i64) -> i64 {
    f(n)
}

fn main() -> i64 {
    # Predicate violation: pass an i64 literal where fn(i64) -> i64
    # is expected. Type checker rejects with E001 'expected fn(i64)->i64,
    # found i64'.
    apply(42, 10)
}
