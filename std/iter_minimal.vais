# Minimal test
F iter_count(start: i64, end: i64) -> i64 {
    I end > start {
        end - start
    } E {
        0
    }
}

F iter_sum(start: i64, end: i64) -> i64 {
    I end <= start {
        0
    } E {
        n := end - 1
        k := start
        n_sum := n * (n + 1) / 2
        k_sum := k * (k - 1) / 2
        n_sum - k_sum
    }
}
