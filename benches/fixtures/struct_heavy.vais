# Struct benchmark - structure allocation and field access

struct Point {
    x: i64,
    y: i64
}

struct Rectangle {
    top_left: Point,
    width: i64,
    height: i64
}

struct Circle {
    center: Point,
    radius: i64
}

fn point_new(x: i64, y: i64)->Point = Point { x: x, y: y }

fn point_distance_squared(p1: Point, p2: Point)->i64 =
    (p1.x - p2.x) * (p1.x - p2.x) + (p1.y - p2.y) * (p1.y - p2.y)

fn rect_area(r: Rectangle)->i64 = r.width * r.height

fn circle_area_approx(c: Circle)->i64 = 3 * c.radius * c.radius

fn main()->i64 = point_distance_squared(point_new(0, 0), point_new(3, 4)) +
    rect_area(Rectangle { top_left: point_new(0, 0), width: 10, height: 20 }) +
    circle_area_approx(Circle { center: point_new(0, 0), radius: 5 })
