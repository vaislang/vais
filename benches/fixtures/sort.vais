# Sort benchmark - algorithm comparisons

fn factorial(n: i64)->i64 = n < 2 ? 1 : n * @(n - 1)

fn gcd(a: i64, b: i64)->i64 = b == 0 ? a : @(b, a % b)

fn is_prime(n: i64)->bool = n <= 1 ? false : check_prime(n, 2)

fn check_prime(n: i64, i: i64)->bool =
    i * i > n ? true :
    n % i == 0 ? false :
    @(n, i + 1)

fn sum_primes(limit: i64)->i64 = sum_primes_helper(limit, 2, 0)

fn sum_primes_helper(limit: i64, n: i64, sum: i64)->i64 =
    n > limit ? sum :
    is_prime(n) ? @(limit, n + 1, sum + n) :
    @(limit, n + 1, sum)

fn main()->i64 = factorial(10) + gcd(48, 18) + sum_primes(50)
