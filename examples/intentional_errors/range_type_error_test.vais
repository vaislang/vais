# Test Range type errors

fn test_invalid_range() -> i64 {
    # This should cause a type error - can't use string in range
    r := "hello"..10
    0
}

fn main() -> i64 {
    test_invalid_range()
}
