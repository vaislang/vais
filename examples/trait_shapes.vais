# Trait Shapes — Polymorphism with traits
# Demonstrates: trait definition (W), implementation (X), generic bounds
#
# Run: vaisc run examples/trait_shapes.vais

trait Describable {
    fn describe(&self) -> str
    fn value(&self) -> i64
}

struct Circle {
    radius: i64
}

struct Square {
    side: i64
}

struct Triangle {
    base: i64,
    height: i64
}

impl Circle: Describable {
    fn describe(&self) -> str { "Circle" }
    fn value(&self) -> i64 {
        # Approximate area: pi * r^2 ~ 3 * r^2
        3 * self.radius * self.radius
    }
}

impl Square: Describable {
    fn describe(&self) -> str { "Square" }
    fn value(&self) -> i64 {
        self.side * self.side
    }
}

impl Triangle: Describable {
    fn describe(&self) -> str { "Triangle" }
    fn value(&self) -> i64 {
        self.base * self.height / 2
    }
}

fn main() -> i64 {
    c := Circle { radius: 5 }
    s := Square { side: 4 }
    t := Triangle { base: 6, height: 3 }

    puts("=== Trait Shapes ===")

    cv := c.value()
    puts("Circle(r=5) area ~ {cv}")

    sv := s.value()
    puts("Square(s=4) area = {sv}")

    tv := t.value()
    puts("Triangle(b=6,h=3) area = {tv}")

    total := cv + sv + tv
    puts("Total area = {total}")

    # 75 + 16 + 9 = 100
    I total == 100 { 0 } else { 1 }
}
