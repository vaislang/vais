# Phase 44 Cross-verify: Async/Await basics (Phase 43)
# Tests async function declaration, await, and yield

A fn compute(x: i64) -> i64 {
    x * 2
}

A fn step1(x: i64) -> i64 {
    x + 5
}

A fn chained(x: i64) -> i64 {
    step1(x).await
}

fn main() -> i64 {
    result := compute(21).await
    r2 := chained(37).await
    (result + r2) - 84
}
