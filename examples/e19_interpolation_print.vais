# expect: 0
# String interpolation in print: "{name}" / "{n}" splice values into output.
# (Vais uses {x} interpolation directly in string literals — no format! macro.)
fn main() -> Int {
    let name = "vais"
    let n = 3
    print("lang={name} n={n}")
    return 0
}
