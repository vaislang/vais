# IPv6 networking test example
# Demonstrates IPv6 socket support
#
# Note: This example demonstrates the API. The full implementation would use std/net
# but the module import system currently has limitations with extension methods.

F main() -> i64 {
    puts("========================================")
    puts("IPv6 Network Socket Test")
    puts("========================================")
    puts("")
    puts("This example demonstrates IPv6 networking APIs:")
    puts("")
    puts("1. Address Validation:")
    puts("   is_valid_ip6(\"::1\") -> checks if IPv6 address is valid")
    puts("")
    puts("2. TCP Listener:")
    puts("   listener := TcpListener.bind6(9090)")
    puts("   Creates IPv6 TCP listener on port 9090")
    puts("")
    puts("3. TCP Client:")
    puts("   stream := TcpStream.connect6(\"::1\", 8080)")
    puts("   Connects to IPv6 address")
    puts("")
    puts("4. UDP Socket:")
    puts("   socket := UdpSocket.bind6(9091)")
    puts("   Creates IPv6 UDP socket")
    puts("")
    puts("5. Low-level C-style API:")
    puts("   tcp_listen6(port) -> fd")
    puts("   tcp_connect6(host, port) -> fd")
    puts("   udp_bind6(port) -> fd")
    puts("")
    puts("For the full implementation, see std/net.vais")
    puts("========================================")
    0
}
