# Test - no impl methods, just enum and main function use

enum SimpleOption<T> {
    None,
    Some(T)
}

fn main() -> i64 {
    x := Some(42)
    match x {
        Some(v) => v,
        None => 0
    }
}
