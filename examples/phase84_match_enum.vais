# Phase 84: Enum matching
enum Shape {
    Circle,
    Square,
    Triangle
}

fn classify(s: i64) -> i64 {
    match s {
        0 => 10,
        1 => 20,
        2 => 30,
        _ => 0
    }
}

fn main() -> i64 {
    a := classify(0)
    b := classify(1)
    c := classify(2)
    puts("enum matching done")
    a + b + c
}
