# Generic function test (simple identity)
fn identity<T>(x: T) -> T = x

fn main() -> i64 {
    puts("Testing generics:")

    a := identity(42)
    puts("identity(42) =")
    putchar(a / 10 + 48)
    putchar(a % 10 + 48)
    putchar(10)

    0
}
