# Fibonacci function with self-recursion
fn fib(n: i64) -> i64 = n < 2 ? n : @(n - 1) + @(n - 2)

# Simple addition
fn add(a: i64, b: i64) -> i64 = a + b

# Main: compute fib(10) = 55
fn main() -> i64 = fib(10)
