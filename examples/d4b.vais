# expect: 9
fn largest(items: List<Int>) -> Int {
    let mut max = items[0]
    for x in items {
        if x > max {
            max = x
        }
    }
    return max
}
fn main() -> Int {
    let v = list()
    v.push(3)
    v.push(9)
    v.push(2)
    v.push(7)
    return largest(v)
}
