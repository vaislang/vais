# expect: 42

fn main() -> Int {
    let scores: Map<Int,Int> = {}
    let other: Map<Int,Int> = {}
    other.insert(4, 40)
    scores = other
    other.insert(4, 1)
    return scores.get(4, 0) + scores.len() + scores.contains(4)
}
