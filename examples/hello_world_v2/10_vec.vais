# Hello World #10: Vec<i64> push, len, index
use std/vec.{vec_new};

fn main() -> i64 {
    v: Vec<i64> := mut vec_new()
    LF i: 1..=5 {
        v.push(i)
    }
    sum: i64 := mut 0
    LF i: 0..v.len() {
        sum = sum + v[i]
    }
    return sum    # exit 15 (1+2+3+4+5)
}
