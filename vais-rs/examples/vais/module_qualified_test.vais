// Module qualified call test
use lib.math

print("Module qualified call test:")

// Direct call (imported name)
let a = add(5, 3) :
print(a)

// Qualified call with module prefix
let b = math.mul(4, 7) :
print(b)

// Combined
let c = add(1, 2) + math.mul(3, 4) :
print(c)

print("Module test passed!")
