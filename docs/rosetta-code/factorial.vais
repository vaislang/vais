// Factorial
// Computes n! using recursion

fn factorial(n: i64) -> i64 {
    I n <= 1 {
        return 1
    }
    return n * @(n - 1)
}

fn main() -> i64 {
    println("Factorial values:")
    L i := 0; i <= 10; i := i + 1 {
        println("{}! = {}", i, factorial(i))
    }
    return 0
}
