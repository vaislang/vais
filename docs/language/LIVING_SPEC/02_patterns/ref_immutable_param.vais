# LIVING_SPEC/02_patterns/ref_immutable_param.vais
# Key concept: `&T` 파라미터는 immutable 참조. 원본 소유권 유지.

struct Point { x: i64, y: i64 }

fn sum_coords(p: &Point) -> i64 {
    p.x + p.y
}

fn main() -> i64 {
    p := Point { x: 3, y: 4 }
    sum_coords(&p)
}
