# LIVING_SPEC/02_patterns/pattern_wildcard.vais
# Key concept: `_` wildcard catches any value without binding.

fn classify(n: i64) -> i64 {
    match n {
        0 => 10,
        1 => 20,
        _ => 99
    }
}

fn main() -> i64 {
    a := classify(0)
    b := classify(42)
    a + b
}
