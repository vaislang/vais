# LIVING_SPEC/02_patterns/match_tuple_destructure.vais
# Key concept: 튜플 패턴으로 여러 값을 한 번에 분해.

F classify_point(p: (i64, i64)) -> i64 {
    M p {
        (0, 0) => 0,
        (0, _) => 1,
        (_, 0) => 2,
        _ => 3
    }
}

F main() -> i64 {
    a := classify_point((0, 0))
    b := classify_point((3, 4))
    a + b
}
