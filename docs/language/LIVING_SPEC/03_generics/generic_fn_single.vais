# LIVING_SPEC/03_generics/generic_fn_single.vais
# Key concept: F name<T>(x: T) -> T 단일 타입 파라미터 제네릭 함수.
# 호출 시 타입은 인자로부터 자동 추론(monomorphization).

F id<T>(x: T) -> T = x

F main() -> i64 {
    n := id(10)
    s := id("hello")
    id(n)
}
