# LIVING_SPEC/03_generics/generic_function_return.vais
# Key concept: 제네릭 타입을 반환하는 함수 — 호출부에서 T가 구체화됨.

struct Pair<T> { first: T, second: T }

fn make_pair<T>(a: T, b: T) -> Pair<T> {
    Pair { first: a, second: b }
}

fn main() -> i64 {
    p := make_pair(10, 20)
    p.first + p.second
}
