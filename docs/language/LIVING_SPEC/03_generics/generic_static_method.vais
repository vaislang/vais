# LIVING_SPEC/03_generics/generic_static_method.vais
# Key concept: 제네릭 struct의 static method (X T { F static_fn ... }).

struct Wrapper<T> {
    inner: T
}

impl Wrapper<T> {
    fn unwrap(self) -> T {
        self.inner
    }
}

fn main() -> i64 {
    w := Wrapper { inner: 42 }
    w.unwrap()
}
