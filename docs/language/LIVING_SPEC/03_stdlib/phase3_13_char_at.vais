# LIVING_SPEC/03_stdlib/phase3_13_char_at.vais
# Key concept: Phase 3.13 / B.2 — `s.char_at(i)` returns `u8` (byte at index).
# Resolved via inkwell str-method dispatch (B.2, 2026-04-21).

fn main() -> i64 {
    s := "hello"
    s.char_at(1) as i64
}
