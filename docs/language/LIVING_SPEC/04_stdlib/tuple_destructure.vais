# LIVING_SPEC/04_stdlib/tuple_destructure.vais
# Key concept: 튜플 값을 M으로 분해.

fn swap_add(t: (i64, i64)) -> i64 {
    match t {
        (a, b) => b + a
    }
}

fn main() -> i64 {
    swap_add((3, 7))
}
