# LIVING_SPEC/05_idioms/idiom_string_interp.vais
# Key concept: ❌ 문자열 연결 vs ✅ 보간
# ✅ DO: "hello {name}" — 가독성 높음, 파싱 에러 방지.
# ❌ DON'T: "hello " + name — 일부 언어와 달리 명시적 변환 필요.

fn main() -> i64 {
    name := "world"
    msg := "hello {name}"
    msg.len()
}
