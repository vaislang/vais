# LIVING_SPEC/01_keywords/L_loop_break.vais
# Key concept: L { ... B } 무한 루프 + break.
# L 은 무한 반복. I 조건이 만족되면 B 로 탈출.

F count_until(limit: i64) -> i64 {
    n := mut 0
    L {
        n = n + 1
        I n >= limit {
            B
        }
    }
    n
}

F main() -> i64 {
    count_until(5)
}
