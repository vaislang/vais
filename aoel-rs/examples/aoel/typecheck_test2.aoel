// Simple type inference test cases (without higher-order functions)

// 1. Nested function calls
double(x) = x * 2
triple(x) = x * 3
compose(x) = double(triple(x))

// 2. Mutual recursion
is_even(n) = n == 0 ? true : is_odd(n - 1)
is_odd(n) = n == 0 ? false : is_even(n - 1)

// 3. Complex arithmetic with type inference
quadratic(a, b, c, x) = a * x * x + b * x + c

// 4. Conditional with type inference
abs(x) = x < 0 ? -x : x

// 5. Multiple recursive calls
tree_sum(n) = n <= 0 ? 0 : n + tree_sum(n - 1) + tree_sum(n - 2)

// Test expression
compose(5) + quadratic(1, 2, 3, 4) + abs(-10) + tree_sum(5)
