[package]
name = "vais-plugin"
version = "0.1.0"
edition = "2021"
authors = ["Steve"]
description = "Plugin system for Vais compiler extensions"
license = "MIT"

[dependencies]
vais-ast = { path = "../vais-ast" }
libloading = "0.8"
toml = "0.8"
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
tempfile = "3.10"
