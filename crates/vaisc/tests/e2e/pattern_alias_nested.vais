# Nested pattern alias test
enum Option<T> {
    Some(T),
    None
}

fn main() -> i64 {
    x := (Some(5), Some(10))
    match x {
        both @ (first @ Some(a), second @ Some(b)) => a + b,
        _ => 0
    }
}
