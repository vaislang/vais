fn identity<T>(x: T) -> T = x

struct Box<T> {
    value: T
}

fn get_value<T>(b: Box<T>) -> T = b.value

fn map<A, B>(opt: Option<A>, f: fn(A) -> B) -> Option<B> {
    match opt {
        Some(x) => Some(f(x)),
        None => None
    }
}
