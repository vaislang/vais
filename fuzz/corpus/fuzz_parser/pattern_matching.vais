fn match_test(x: i64) -> str {
    match x {
        0 => "zero",
        1 | 2 | 3 => "small",
        n I n < 10 => "medium",
        _ => "large"
    }
}

fn destructure(p: Point) -> i64 {
    match p {
        Point { x: 0, y } => y,
        Point { x, y: 0 } => x,
        Point { x, y } => x + y
    }
}
