# Multi-function test for Stage 1 compiler
# Tests: multiple functions, function calls

# Helper function: add 10 to input
F add_ten(x: i64) -> i64 = x + 10

# Another helper: double the input
F double(x: i64) -> i64 = x + x

# Combines both helpers - calc(5) = add_ten(double(5)) = add_ten(10) = 20
F calc(x: i64) -> i64 = add_ten(double(x))

# Entry point: expression body (no block)
F main() -> i64 = calc(5)
